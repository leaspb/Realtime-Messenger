# Руководство по Развертыванию - Realtime Messenger

Это руководство охватывает развертывание приложения Realtime Messenger на различных хостинг-платформах с акцентом на бесплатные и недорогие варианты.

## Содержание

- [Требования к Архитектуре](#требования-к-архитектуре)
- [Варианты Развертывания](#варианты-развертывания)
  - [1. Railway (Рекомендуется - Бесплатный Tier)](#1-railway-рекомендуется---бесплатный-tier)
  - [2. Render (Бесплатный Tier)](#2-render-бесплатный-tier)
  - [3. Fly.io (Бесплатный Tier)](#3-flyio-бесплатный-tier)
  - [4. Vercel (Бесплатный - Только Фронтенд)](#4-vercel-бесплатный---только-фронтенд)
  - [5. Самостоятельный Хостинг (VPS)](#5-самостоятельный-хостинг-vps)
- [Переменные Окружения](#переменные-окружения)
- [После Развертывания](#после-развертывания)
- [Сравнение Стоимости](#сравнение-стоимости)
- [Решение Проблем](#решение-проблем)

---

## Требования к Архитектуре

Приложение Realtime Messenger требует:

- **Node.js** runtime (v18+)
- Поддержка **WebSocket** (постоянные соединения)
- **HTTP/HTTPS** сервер
- База данных **PostgreSQL** (опционально - сейчас не используется, но доступна для будущих функций)
- **SSL/TLS** для продакшена (требуется для доступа к микрофону в браузерах)

---

## Варианты Развертывания

### 1. Railway (Рекомендуется - Бесплатный Tier)

Railway предлагает щедрый бесплатный тариф ($5 кредитов/месяц), идеально подходящий для этого приложения.

#### Стоимость: **$0-5/месяц**
- Бесплатно: $5 кредитов/месяц (хобби-проекты)
- Платно: $5/месяц после использования кредитов

#### Преимущества:
- Простое развертывание из GitHub
- Автоматический HTTPS
- Поддержка WebSocket
- PostgreSQL включена
- Отлично подходит для хобби-проектов

#### Недостатки:
- Ограниченный бесплатный тариф
- Биллинг на основе кредитов

#### Шаги по Развертыванию:

1. **Создать Аккаунт**
   ```
   Перейти на: https://railway.app
   Зарегистрироваться через GitHub
   ```

2. **Создать Новый Проект**
   - Нажать "New Project"
   - Выбрать "Deploy from GitHub repo"
   - Подключить ваш репозиторий
   - Выбрать репозиторий `Realtime-Messenger`

3. **Настроить Параметры Сборки**

   Railway автоматически определяет Node.js. Добавьте эти настройки:

   **Команда сборки:**
   ```bash
   npm install && npm run build
   ```

   **Команда запуска:**
   ```bash
   npm start
   ```

4. **Добавить Переменные Окружения**

   В Railway Dashboard → Variables:
   ```env
   NODE_ENV=production
   PORT=5000
   ```

5. **Сгенерировать Домен**
   - Перейти в Settings → Domains
   - Нажать "Generate Domain"
   - Railway предоставит: `your-app.up.railway.app`

6. **Развернуть**
   - Railway автоматически деплоит при git push
   - Мониторить логи в Dashboard

#### Добавление PostgreSQL (Опционально):

```bash
# В Railway Dashboard:
1. Нажать "New" → "Database" → "PostgreSQL"
2. Railway автоматически создаст DATABASE_URL
3. Ручная настройка не требуется
```

---

### 2. Render (Бесплатный Tier)

Render предлагает полностью бесплатный тариф (с ограничениями).

#### Стоимость: **$0/месяц (Бесплатно)**
- Бесплатный тариф доступен бессрочно
- Платно: $7/месяц для постоянно работающего сервиса

#### Преимущества:
- Настоящий бесплатный тариф (кредитная карта не требуется)
- Автоматический HTTPS
- Подходит для хобби-проектов
- PostgreSQL на бесплатном тарифе

#### Недостатки:
- Бесплатный тариф засыпает после 15 мин неактивности
- Холодный старт может занять 30-60 секунд
- Ограничение 750 часов/месяц на бесплатном тарифе

#### Шаги по Развертыванию:

1. **Создать Аккаунт**
   ```
   Перейти на: https://render.com
   Зарегистрироваться через GitHub
   ```

2. **Создать Web Service**
   - Нажать "New +" → "Web Service"
   - Подключить GitHub репозиторий
   - Выбрать `Realtime-Messenger`

3. **Настроить Сервис**

   **Name:** `realtime-messenger`

   **Environment:** `Node`

   **Build Command:**
   ```bash
   npm install && npm run build
   ```

   **Start Command:**
   ```bash
   npm start
   ```

   **Plan:** Free

4. **Переменные Окружения**
   ```env
   NODE_ENV=production
   PORT=5000
   ```

5. **Создать render.yaml (Опционально)**

   Создать `render.yaml` в корне проекта для более простого развертывания:

   ```yaml
   services:
     - type: web
       name: realtime-messenger
       env: node
       plan: free
       buildCommand: npm install && npm run build
       startCommand: npm start
       envVars:
         - key: NODE_ENV
           value: production
         - key: PORT
           value: 5000
   ```

6. **Развернуть**
   - Нажать "Create Web Service"
   - Render предоставит: `https://realtime-messenger.onrender.com`

#### Важное Примечание:
Бесплатный тариф засыпает после 15 мин. Первый запрос после сна занимает ~30с для пробуждения.

---

### 3. Fly.io (Бесплатный Tier)

Fly.io предоставляет бесплатный тариф с хорошей производительностью для хобби-проектов.

#### Стоимость: **$0-5/месяц**
- Бесплатно: 3 shared-cpu VM, 3GB хранилища
- Платно: ~$2/месяц для лучшей производительности

#### Преимущества:
- Щедрый бесплатный тариф
- Глобальная edge сеть
- Хорошая поддержка WebSocket
- Быстрое развертывание
- PostgreSQL доступна

#### Недостатки:
- Требуется кредитная карта (даже для бесплатного тарифа)
- Развертывание через CLI (не GUI)

#### Шаги по Развертыванию:

1. **Установить Fly CLI**
   ```bash
   # macOS
   brew install flyctl

   # Linux
   curl -L https://fly.io/install.sh | sh

   # Windows
   iwr https://fly.io/install.ps1 -useb | iex
   ```

2. **Войти**
   ```bash
   fly auth login
   ```

3. **Инициализировать Приложение**
   ```bash
   cd /path/to/Realtime-Messenger
   fly launch
   ```

   Ответить на вопросы:
   - Имя приложения: `realtime-messenger` (или ваш выбор)
   - Регион: Выбрать ближайший к пользователям
   - PostgreSQL: Нет (если не нужна)
   - Развернуть сейчас: Нет (сначала настроить)

4. **Настроить fly.toml**

   Fly генерирует `fly.toml`. Обновить его:

   ```toml
   app = "realtime-messenger"
   primary_region = "iad"

   [build]
     [build.args]
       NODE_ENV = "production"

   [env]
     PORT = "8080"
     NODE_ENV = "production"

   [http_service]
     internal_port = 8080
     force_https = true
     auto_stop_machines = true
     auto_start_machines = true
     min_machines_running = 0

   [[vm]]
     cpu_kind = "shared"
     cpus = 1
     memory_mb = 256
   ```

5. **Развернуть**
   ```bash
   fly deploy
   ```

6. **Открыть Приложение**
   ```bash
   fly open
   ```

   URL: `https://realtime-messenger.fly.dev`

7. **Просмотр Логов**
   ```bash
   fly logs
   ```

---

### 4. Vercel (Бесплатный - Только Фронтенд)

Vercel отлично подходит для фронтенда, но **не может хостить WebSocket сервер**.

#### Стоимость: **$0/месяц (Бесплатно) + отдельный бэкенд**

#### Сценарий Использования:
Развернуть фронтенд на Vercel, бэкенд в другом месте (Railway/Render).

#### Преимущества:
- Отличная производительность фронтенда
- Автоматические развертывания
- Глобальный CDN
- Отличный DX

#### Недостатки:
- Не может запускать WebSocket сервер
- Требуется отдельное развертывание бэкенда

#### Шаги по Развертыванию:

1. **Сначала Развернуть Бэкенд**
   - Использовать Railway/Render/Fly.io для бэкенда
   - Получить URL бэкенда: `https://api.your-domain.com`

2. **Обновить URL WebSocket**

   В `client/src/hooks/use-webrtc.ts`:
   ```typescript
   const wsUrl = `wss://api.your-domain.com/ws`; // Изменить с window.location.host
   ```

3. **Развернуть на Vercel**
   ```bash
   npm install -g vercel
   vercel login
   vercel
   ```

4. **Настроить**
   - Framework: Vite
   - Build: `npm run build`
   - Output: `dist`

**Не Рекомендуется**, если только вам не нужен глобальный CDN для статических ресурсов.

---

### 5. Самостоятельный Хостинг (VPS)

Для полного контроля, разверните на VPS.

#### Стоимость: **$5-10/месяц**
- DigitalOcean: $6/месяц (1GB RAM)
- Linode: $5/месяц (1GB RAM)
- Vultr: $6/месяц (1GB RAM)
- Hetzner: €4.5/месяц (~$5, базируется в Европе)

#### Преимущества:
- Полный контроль
- Нет ограничений платформы
- Самый дешевый вариант в долгосрочной перспективе
- Можно запускать несколько приложений

#### Недостатки:
- Ручная настройка
- Требуются знания Linux
- Вы управляете обновлениями безопасности
- Нужно настраивать SSL

#### Шаги по Развертыванию:

1. **Получить VPS**
   - Рекомендуется: DigitalOcean, Hetzner
   - ОС: Ubuntu 22.04 LTS
   - Размер: минимум 1GB RAM

2. **Начальная Настройка**
   ```bash
   # SSH на сервер
   ssh root@your-server-ip

   # Обновить систему
   apt update && apt upgrade -y

   # Установить Node.js 20
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs

   # Установить PM2 (менеджер процессов)
   npm install -g pm2

   # Установить Nginx (обратный прокси)
   apt install -y nginx

   # Установить Certbot (SSL)
   apt install -y certbot python3-certbot-nginx
   ```

3. **Развернуть Приложение**
   ```bash
   # Клонировать репозиторий
   cd /var/www
   git clone https://github.com/yourusername/Realtime-Messenger.git
   cd Realtime-Messenger

   # Установить зависимости
   npm install

   # Собрать
   npm run build

   # Запустить с PM2
   pm2 start npm --name "realtime-messenger" -- start
   pm2 save
   pm2 startup
   ```

4. **Настроить Nginx**
   ```bash
   nano /etc/nginx/sites-available/realtime-messenger
   ```

   Добавить конфигурацию:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;

       location / {
           proxy_pass http://localhost:5000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }

       location /ws {
           proxy_pass http://localhost:5000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection "Upgrade";
           proxy_set_header Host $host;
       }
   }
   ```

   Включить сайт:
   ```bash
   ln -s /etc/nginx/sites-available/realtime-messenger /etc/nginx/sites-enabled/
   nginx -t
   systemctl restart nginx
   ```

5. **Настроить SSL (Бесплатно с Let's Encrypt)**
   ```bash
   certbot --nginx -d your-domain.com
   ```

6. **Настроить Firewall**
   ```bash
   ufw allow OpenSSH
   ufw allow 'Nginx Full'
   ufw enable
   ```

7. **Автодеплой при git push (Опционально)**
   ```bash
   # Создать скрипт деплоя
   nano /var/www/Realtime-Messenger/deploy.sh
   ```

   ```bash
   #!/bin/bash
   cd /var/www/Realtime-Messenger
   git pull
   npm install
   npm run build
   pm2 restart realtime-messenger
   ```

   ```bash
   chmod +x deploy.sh
   ```

---

## Переменные Окружения

Обязательные переменные окружения для продакшена:

```env
# Обязательные
NODE_ENV=production
PORT=5000

# Опциональные (если будете использовать БД в будущем)
DATABASE_URL=postgresql://user:password@host:5432/dbname
```

---

## После Развертывания

### Тестирование WebSocket Соединения

1. **Открыть Консоль Браузера**
   ```javascript
   const ws = new WebSocket('wss://your-domain.com/ws');
   ws.onopen = () => console.log('Подключено!');
   ws.onerror = (err) => console.error('Ошибка:', err);
   ```

2. **Тестировать Доступ к Микрофону**
   - Убедитесь, что сайт использует HTTPS (требуется для `getUserMedia`)
   - Браузер запросит разрешение на использование микрофона

### Мониторинг

**Railway/Render:**
- Встроенные логи и метрики в панели управления

**Fly.io:**
```bash
fly logs
fly status
```

**VPS:**
```bash
pm2 logs realtime-messenger
pm2 monit
```

---

## Сравнение Стоимости

| Платформа | Бесплатный Tier | Платный Tier | Лучше Для |
|----------|-----------------|--------------|-----------|
| **Railway** | $5 кредитов/мес | $5/мес после | Простое развертывание, хобби-проекты |
| **Render** | Бесплатно (засыпает) | $7/мес | Настоящий бесплатный tier, низкий трафик |
| **Fly.io** | 3 VM бесплатно | ~$2/мес | Глобальный edge, всегда работает бесплатно |
| **VPS** | Н/Д | $5-10/мес | Полный контроль, несколько приложений |

### Рекомендации по Сценариям Использования:

- **Хобби/Обучение:** Render (бесплатно)
- **Портфолио Проект:** Railway ($5 кредитов/мес)
- **Всегда Работает Бесплатно:** Fly.io (3 бесплатных VM)
- **Продакшен:** VPS ($5/мес) или Railway ($5/мес)
- **Несколько Проектов:** VPS ($10/мес для 2GB RAM)

---

## Решение Проблем

### Не Удается Подключиться к WebSocket

**Проблема:** Клиент не может подключиться к WebSocket

**Решения:**
1. Убедитесь, что включен HTTPS (протокол WSS)
2. Проверьте, что firewall разрешает WebSocket (порты 80/443)
3. Проверьте заголовки upgrade для WebSocket в Nginx/прокси
4. Проверьте логи сервера на наличие ошибок соединения

### Доступ к Микрофону Запрещен

**Проблема:** Браузер не предоставляет доступ к микрофону

**Решения:**
1. Убедитесь, что сайт использует HTTPS (не HTTP)
2. Проверьте разрешения браузера
3. Протестируйте в другом браузере
4. Проверьте CORS заголовки, если фронтенд/бэкенд разделены

### Приложение Крашится После Деплоя

**Проблема:** Приложение запускается, затем падает

**Решения:**
1. Проверьте логи: `pm2 logs` или панель платформы
2. Проверьте версию Node.js (v18+)
3. Проверьте, что `npm run build` завершается успешно
4. Убедитесь, что все зависимости установлены
5. Проверьте переменную окружения PORT

### Холодные Старты (Render Free Tier)

**Проблема:** Приложение медленно отвечает после неактивности

**Решения:**
1. Обновиться до платного тарифа ($7/мес)
2. Использовать cron задачу для пинга каждые 14 мин (держит активным)
3. Переключиться на Fly.io (лучший бесплатный tier)

**Скрипт пинга (cron):**
```bash
# Пинговать каждые 14 минут
*/14 * * * * curl https://your-app.onrender.com > /dev/null 2>&1
```

### Проблемы с Подключением к БД

**Проблема:** Не удается подключиться к PostgreSQL

**Решения:**
1. Проверьте формат DATABASE_URL
2. Убедитесь, что БД находится в том же регионе (Railway/Render)
3. Проверьте, что сервис БД запущен
4. Проверьте, что строка подключения содержит правильные учетные данные

---

## Чеклист Безопасности

Перед выходом в продакшен:

- [ ] HTTPS включен (SSL сертификат)
- [ ] Переменные окружения установлены (не захардкожены)
- [ ] CORS настроен при необходимости
- [ ] Rate limiting включен (уже в коде)
- [ ] Валидация ввода работает (уже в коде)
- [ ] WebSocket аутентификация (добавить при необходимости)
- [ ] Настроены бэкапы БД (если используется БД)
- [ ] Установлены правила firewall (только VPS)
- [ ] Обновлены зависимости (`npm audit fix`)

---

## Быстрый Старт: Railway (Проще Всего)

```bash
# 1. Загрузить на GitHub
git add .
git commit -m "Готово к развертыванию"
git push origin main

# 2. Перейти на railway.app
# 3. New Project → Deploy from GitHub
# 4. Выбрать репозиторий
# 5. Добавить переменные окружения:
#    NODE_ENV=production
#    PORT=5000
# 6. Развернуть!

# Ваше приложение будет доступно по адресу:
# https://your-app.up.railway.app
```

---

## Нужна Помощь?

- Railway: https://railway.app/help
- Render: https://render.com/docs
- Fly.io: https://fly.io/docs
- DigitalOcean: https://docs.digitalocean.com

---

**Рекомендуемый Путь для Начинающих:**

1. Начать с **Render Free Tier** (кредитная карта не нужна)
2. Если нужен постоянно работающий сервис, перейти на **Railway** ($5/мес)
3. Для продакшена рассмотреть **VPS** ($5-10/мес)

Удачного развертывания! 🚀
