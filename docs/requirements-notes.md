## Packages
framer-motion | For smooth UI transitions and layout animations
date-fns | For timestamp formatting

## Notes
Using native WebSocket for signaling.
WebRTC implementation uses a Mesh topology (everyone connects to everyone).
Audio elements will be created dynamically for each remote peer.
